# BuildProcure MCP Setup Guide

## Prerequisites

- Python 3.8+
- pip (Python package manager)
- GitHub Personal Access Token with `repo` scope

## Installation

### 1. Clone the Repository

```bash
git clone https://github.com/buildprocure/buildprocure_mcp_config.git
cd buildprocure_mcp_config